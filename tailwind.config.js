// Tailwind v4: Config is optional and CSS-first. This file is intentionally minimal to avoid
// conflicts with the v3-style config. Theme and content sources are defined in src/index.css via
// `@theme` and `@source` directives.
export default {
  // Intentionally empty for v4 setup
}
